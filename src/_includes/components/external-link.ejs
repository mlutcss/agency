<%
	const iconStyles = {
		width: '',
		height: '',
		fill: ''
	}

	if (it.name === "X") {
		iconStyles.width = "24";
		iconStyles.height = "24";
	} else if (it.name === 'Github') {
		iconStyles.width = "26";
		iconStyles.height = "26";
	} else {
		iconStyles.width = "28";
		iconStyles.height = "28";
	}

	if (it.icon === 'telegram' || it.icon === 'mail') {
		iconStyles.fill = ''
	} else {
		iconStyles.fill = 'Fi-$accent900 Fio1 ^logo:h:_Fi-$brand -Ts'
	}
%>

<a href="<%= it.link %>" target="_blank" class="-Ctx-logo Ps W100p D-f Jc-c Ai-c">
	<svg class="<%= iconStyles.fill %>" width="<%= iconStyles.width %>" height="<%= iconStyles.height %>">
		<use href="/assets/img/icons.svg#<%= it.icon %>"></use>
	</svg>
</a>